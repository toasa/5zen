// for RV32I
pub const REG_SIZE: usize = 32;
pub const REG_NUM: usize = REG_SIZE;
pub const REG_TYPE = i32;
pub const PROG_TYPE = u32;
// pub const MEM_SIZE: usize = 1 << REG_SIZE;
pub const MEM_SIZE: usize = 65536;
// when pc equals `RET_ADDR`, cpu is halt.
pub const RET_ADDR: usize = MEM_SIZE - 1;